# Runtime dependencies for provisioning and inventory automation.
paramiko>=4.0.0
cryptography>=46.0.0
requests>=2.32.0

# Test and reporting stack.
pytest>=9.0.0
pytest-playwright>=0.8.0
allure-pytest>=2.16.0

# Quality gates. The stub packages are what let mypy run in strict mode
# against paramiko and requests, which ship no inline types.
pylint>=4.0.0
mypy>=1.11
types-paramiko
types-requests
